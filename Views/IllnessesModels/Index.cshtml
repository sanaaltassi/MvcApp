@model IEnumerable<MvcApp.Models.IllnessesModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Illnesses</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IllnessesType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IllnessesId)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IllnessesType)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.IllnessesId)
                </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Name">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Name">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Name">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
